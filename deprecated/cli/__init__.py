"""
Agent² CLI Package

This package provides a command-line interface for the Agent² multi-agent
orchestration system.

Usage:
    cd cli && python agent_chain.py "Your prompt here"
    cd cli && python agent_chain.py --test-cli
    cd cli && python agent_chain.py --list-agents
    
    # Or use the shell wrapper:
    ./cli/chain-agents.sh "Your prompt here"
"""
